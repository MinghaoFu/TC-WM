name: cheetah_run
args: []
kwargs: {}

dataset:
  _target_: "datasets.dmc_dset.load_dmc_slice_train_val"
  n_rollout: null
  normalize_action: ${normalize_action}
  data_path: ${oc.env:TCWM_DATA_ROOT,./data}/newt_rollouts/cheetah-run
  split_ratio: 0.8
  image_size: ${img_size}
  transform:
    _target_: "datasets.img_transforms.default_transform"
    img_size: ${img_size}

decoder_path: null
num_workers: 16
